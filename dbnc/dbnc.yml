{
    bn_abstr_train_size: 20000,
    # feat_extr_train_size: .5,
    # feats: { decomp: 'pca', n_components: 8, svd_solver: 'randomized' },
    feats: { decomp: 'pca', n_components: 2, svd_solver: 'full' },
    # feats: 2,
    # feats: 0.5,
    #feats: { decomp: 'ica', n_components: 2, max_iter: 5000, tol: 0.01 },
    # feats: { decomp: 'ica', n_components: 3, max_iter: 10000, tol: 0.01 },
    # feats: '(lambda li: 3 if li <= 2 else 4)',
    # feats: '(lambda li: 0.9 if li > 5 else 0.7 if li > 4 else 0.5 if li > 2 else 0.1)',
    # feats: '(lambda li: { "decomp": "ica", "n_components": 3 } if li <= 2 else { "n_components": 4 })',
    # discr: { n_bins: 0, extended: True, strategy: "uniform" },
    discr: 2, 
    # discr: { strategy: 'kde', kde_plot_spaces: 'dens', kde_plot_incorrectly_classified: True, extended: True },
    # discr: { strategy: 'kde', kde_plot_spaces: 'dens', extended: True },
    # discr: '(lambda li: { "n_bins": 4, "strategy": "uniform" } if li in (1,) else None)',
    # discr: '(lambda li: "bin" if li == 3 else dict(n_bins=3, strategy="uniform"))',
    # report_on_feature_extractions: True,
    bn_abstr_n_jobs: 8,
    discr_n_jobs: -1,
    dump_bn_with_trained_dataset_distribution: True,
    dump_bn_with_final_dataset_distribution: True,
    verbose: 2,
}
